# GitHub 上傳指南

## 📋 專案結構說明

### 前端專案（不動，不上傳）
- 前端檔案保留在原本位置，**不需要上傳到後端 GitHub**
- 前端有自己的專案結構

### 後端 + 後台管理系統專案（同一個專案，一起上傳）
- **後端 API** 和 **後台管理系統** 都在 `backend/` 資料夾中
- 這是**同一個專案**，**同一個 GitHub Repository**，**一起部署到 Zeabur**

---

## ✅ 需要上傳到 GitHub 的檔案

### 📁 backend/ 資料夾（整個資料夾都要上傳）

```
backend/
├── src/                          ✅ 必須上傳
│   ├── db/                       ✅ 必須上傳
│   │   ├── database.ts
│   │   └── migrate.ts
│   ├── models/                   ✅ 必須上傳
│   │   ├── Article.ts
│   │   └── Profile.ts
│   ├── routes/                   ✅ 必須上傳
│   │   ├── profiles.ts          (公開 API)
│   │   ├── articles.ts          (公開 API)
│   │   ├── gemini.ts            (公開 API)
│   │   └── admin.ts             ⭐ 後台管理系統 API
│   ├── types.ts
│   └── server.ts                 (主伺服器，包含所有路由)
├── package.json                  ✅ 必須上傳
├── package-lock.json             ✅ 必須上傳
├── tsconfig.json                 ✅ 必須上傳
├── README.md                     ✅ 必須上傳
├── API.md                        ✅ 必須上傳（API 文檔）
├── Dockerfile                    ✅ 必須上傳（備用）
├── .dockerignore                 ✅ 必須上傳
├── .gitignore                    ✅ 必須上傳
└── zeabur.json                   ✅ 必須上傳（Zeabur 配置）
```

### 📁 根目錄（部署相關檔案）

```
根目錄/
├── .zeabur.yaml                  ✅ 必須上傳（Zeabur 部署配置）
├── DEPLOYMENT.md                 ✅ 建議上傳（部署指南）
└── QUICKSTART.md                 ✅ 建議上傳（快速開始）
```

---

## ❌ 不需要上傳到 GitHub 的檔案

以下檔案會被 `.gitignore` 自動忽略，**不需要手動上傳**：

```
backend/
├── node_modules/                 ❌ 不上傳（npm install 會自動安裝）
├── dist/                         ❌ 不上傳（npm run build 會自動產生）
├── data/                         ❌ 不上傳（資料庫檔案，部署時自動建立）
├── .env                          ❌ 不上傳（敏感資訊，在 Zeabur 設定環境變數）
├── *.log                         ❌ 不上傳（日誌檔案）
└── .DS_Store                     ❌ 不上傳（系統檔案）
```

---

## 🎯 後台管理系統說明

### 後台管理系統位置

**後台管理系統** 位於：`backend/src/routes/admin.ts`

這是**後端 API 的一部分**，與後端在同一個專案中。

### 後台管理系統功能

所有後台管理功能都在 `/api/admin/*` 路由下：

#### 1. 統計資訊
- `GET /api/admin/stats` - 取得後台統計資訊

#### 2. Profile 管理（上架新茶）
- `GET /api/admin/profiles` - 取得所有 profiles
- `GET /api/admin/profiles/:id` - 取得單一 profile
- `POST /api/admin/profiles` - **上架新茶** ⭐
- `PUT /api/admin/profiles/:id` - **編輯茶茶** ⭐
- `PATCH /api/admin/profiles/:id` - 部分更新
- `DELETE /api/admin/profiles/:id` - **下架茶茶** ⭐
- `POST /api/admin/profiles/batch` - **批量操作** ⭐

#### 3. Article 管理（發布新文章）
- `GET /api/admin/articles` - 取得所有 articles
- `GET /api/admin/articles/:id` - 取得單一 article
- `POST /api/admin/articles` - **發布新文章** ⭐
- `PUT /api/admin/articles/:id` - **編輯文章** ⭐
- `DELETE /api/admin/articles/:id` - **刪除文章** ⭐
- `POST /api/admin/articles/batch` - **批量操作** ⭐

### 後台管理系統與後端的關係

```
backend/
└── src/
    └── server.ts                 (主伺服器)
        ├── /api/profiles         (公開 API - 前端使用)
        ├── /api/articles         (公開 API - 前端使用)
        ├── /api/gemini           (公開 API - AI 功能)
        └── /api/admin            ⭐ 後台管理系統 API
            ├── /admin/profiles   (管理 Profiles)
            ├── /admin/articles   (管理 Articles)
            └── /admin/stats       (統計資訊)
```

**重點：**
- 後端 API 和後台管理系統在**同一個 Express 伺服器**中
- 後台管理系統是後端的一部分，不是獨立專案
- 部署時會一起部署到 Zeabur

---

## 📝 上傳步驟

### 1. 確認 .gitignore 正確

`.gitignore` 應該包含：
```
node_modules/
dist/
.env
data/
*.log
.DS_Store
```

### 2. 初始化 Git（如果還沒有）

```bash
cd backend
git init
```

### 3. 檢查要上傳的檔案

```bash
# 查看會被上傳的檔案（排除 .gitignore 的檔案）
git status
```

### 4. 提交檔案

```bash
# 加入所有檔案（.gitignore 會自動過濾）
git add .

# 提交
git commit -m "Initial commit: Backend API with admin system"

# 在 GitHub 建立 Repository，然後：
git remote add origin https://github.com/your-username/your-repo.git
git branch -M main
git push -u origin main
```

---

## 🔍 驗證上傳內容

上傳後，GitHub Repository 應該包含：

```
your-repo/
├── backend/
│   ├── src/
│   │   ├── db/
│   │   ├── models/
│   │   ├── routes/
│   │   │   ├── profiles.ts
│   │   │   ├── articles.ts
│   │   │   ├── gemini.ts
│   │   │   └── admin.ts          ⭐ 後台管理系統
│   │   ├── types.ts
│   │   └── server.ts
│   ├── package.json
│   ├── tsconfig.json
│   ├── README.md
│   ├── API.md
│   ├── Dockerfile
│   ├── .gitignore
│   └── zeabur.json
├── .zeabur.yaml
├── DEPLOYMENT.md
└── QUICKSTART.md
```

**不應該包含：**
- ❌ `node_modules/`
- ❌ `dist/`
- ❌ `.env`
- ❌ `data/`

---

## 🚀 部署到 Zeabur

上傳到 GitHub 後：

1. 在 Zeabur Dashboard 選擇你的 GitHub Repository
2. Zeabur 會自動偵測 `.zeabur.yaml` 配置
3. 設定環境變數：
   - `GEMINI_API_KEY`
   - `NODE_ENV=production`
4. 部署完成後，後端 API 和後台管理系統會一起上線

---

## 📌 重點提醒

1. **前端和後端分開**：前端不動，不上傳到後端 GitHub
2. **後端和後台管理系統在一起**：都在 `backend/` 資料夾，同一個專案
3. **後台管理系統位置**：`backend/src/routes/admin.ts`
4. **部署**：後端和後台管理系統會一起部署到 Zeabur
5. **API 端點**：後台管理系統使用 `/api/admin/*` 路由

